Follow the SHL Assignment PDF as the primary specification.
Use only the SHL Product Catalog for recommendations.
Match the behavior demonstrated in the sample conversations.
Keep the API stateless.
Do not introduce features outside the assignment scope.
Ensure every response conforms to the required JSON schema.
Keep modules focused and avoid mixing responsibilities.